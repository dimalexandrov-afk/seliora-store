import {products} from '@/lib/products';import {notFound} from 'next/navigation';
export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main className="wrap"><div className="pagehead"><div className="eyebrow">SELIORA collection</div><h1>{p.name}</h1><p className="lead">{p.meta}</p></div><div className="story" style={{paddingBottom:80}}><div className="productart" style={{height:560}}/><div><h2 style={{fontFamily:'Georgia,serif',fontWeight:400,fontSize:36}}>{p.price}</h2><p className="lead">Black · Sizes S / M / L / XL</p><div className="placeholder">Product details are placeholders until physical sample approval.<br/><br/>Planned: finish, material composition, toe construction, waistband, care, exact fit chart and launch pricing.</div></div></div></main>}
