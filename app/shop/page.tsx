import Link from 'next/link';import {products} from '@/lib/products';
export default function Shop(){return <main className="wrap"><div className="pagehead"><div className="eyebrow">Collection</div><h1>Shop SELIORA</h1><p className="lead">Placeholder collection while supplier samples are being evaluated.</p></div><div className="products" style={{paddingBottom:80}}>{products.map(p=><Link className="card" href={`/product/${p.slug}`} key={p.slug}><div className="productart"/><div className="cardbody"><div className="name">{p.name}</div><div className="meta">{p.meta}</div><div className="price">{p.price}</div></div></Link>)}</div></main>}
