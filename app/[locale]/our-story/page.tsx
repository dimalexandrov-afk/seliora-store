import {notFound} from 'next/navigation';import {isLocale,copy} from '@/lib/i18n';
export default async function Story({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <main className="wrap"><div className="pagehead"><div className="eyebrow">{t.story}</div><h1>{locale==='bg'?'Чорапогащници без излишни категории.':'Modern hosiery, without unnecessary labels.'}</h1><p className="lead">{locale==='bg'?'SELIORA се изгражда около проста идея: продуктът да се оценява по fit, финиш, усещане и качество. Първата колекция в момента е в етап на физическо тестване.':'SELIORA is being built around a simple idea: judge hosiery by its fit, finish, feel and quality. The first collection is currently in physical product testing.'}</p></div><section className="section story"><div><div className="eyebrow">{t.point}</div><h2>{t.pointTitle}</h2></div><div className="story-copy">{t.pointBody}<br/><br/>{t.wear}</div></section></main>}
