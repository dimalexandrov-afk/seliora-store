import Link from 'next/link';import {notFound} from 'next/navigation';import {isLocale,copy} from '@/lib/i18n';import {products} from '@/lib/products';
export default async function Shop({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <main className="wrap"><div className="pagehead"><div className="eyebrow">{t.collection}</div><h1>{t.shop}</h1><p className="lead">{t.productTest}</p></div><div className="products" style={{paddingBottom:90}}>{products.map((p,i)=><Link className="card" href={`/${locale}/product/${p.slug}`} key={p.slug}><div className={`productart ${i===2?'gloss':''} ${i===3?'opaque':''}`}/><div className="cardbody"><div className="name">{p.name}</div><div className="meta">{p.meta}</div><div className="price"><span className="oldprice">{p.regular}</span>{p.price}</div></div></Link>)}</div></main>}
