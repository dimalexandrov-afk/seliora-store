import {notFound} from 'next/navigation';import {isLocale,copy} from '@/lib/i18n';
export default async function Fit({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <main className="wrap"><div className="pagehead"><div className="eyebrow">{t.fit}</div><h1>{locale==='bg'?'Намери точния fit.':'Find your fit.'}</h1><p className="lead">{locale==='bg'?'Финалните таблици ще бъдат добавени след физическо тестване. SELIORA ще използва ръст, тегло, ханш и талия вместо полови категории.':'Final size tables will be added after physical testing. SELIORA sizing will use height, weight, hip and waist measurements rather than gender categories.'}</p></div><div className="placeholder" style={{marginBottom:90}}>{locale==='bg'?'Интерактивен size finder — следва в следващата версия.':'Interactive size finder — coming in the next version.'}</div></main>}
