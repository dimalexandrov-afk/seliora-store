import {notFound} from 'next/navigation';import {isLocale,copy} from '@/lib/i18n';
export default async function FAQ({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=copy[locale];return <main className="wrap"><div className="pagehead"><div className="eyebrow">{t.faq}</div><h1>{locale==='bg'?'Ясни отговори.':'Questions, answered simply.'}</h1><p className="lead">{locale==='bg'?'Информацията за доставка, връщане, размери, материали и поддръжка ще бъде финализирана преди старта.':'Shipping, returns, sizing, materials and care information will be finalized before launch.'}</p></div></main>}
