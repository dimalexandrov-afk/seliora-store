import Link from 'next/link';import type {Locale} from '@/lib/i18n';import {copy} from '@/lib/i18n';import CartIndicator from './CartIndicator';
export default function LocalizedHeader({locale}:{locale:Locale}){const t=copy[locale];return <><div className="topbar">{t.topbar}</div><div className="navshell"><div className="wrap"><nav className="nav"><div className="navlinks"><Link href={`/${locale}/shop`}>{t.shop}</Link><Link href={`/${locale}/our-story`}>{t.story}</Link><Link href={`/${locale}/fit-guide`}>{t.fit}</Link><Link href={`/${locale}/faq`}>{t.faq}</Link></div><Link className="brand" href={`/${locale}`}>SELIORA</Link><div className="navright"><div className="lang"><Link className={locale==='en'?'active':''} href="/en">EN</Link><Link className={locale==='bg'?'active':''} href="/bg">BG</Link></div><CartIndicator locale={locale} label={t.bag}/></div></nav></div></div></>}
